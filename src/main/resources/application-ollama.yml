spring:
  ai:
    ollama:
      base-url: http://ludwig:11434
      # api-key is not a real OllamaConnectionProperties field and would be silently ignored.
      # The key is injected via the custom OllamaApi bean in AiConfig, sourced from chatbot.api.api-key.
      chat:
        enabled: true
        options:
          model: gemma4:latest
          temperature: 0.3
    openai:
      api-key: ${LLM_API_KEY:}
      chat:
        enabled: false

ocr:
  api:
    url: http://ludwig:11434/api/chat
    model: gemma4:latest
    format: ollama

chatbot:
  api:
    api-key: ${LLM_API_KEY:}

